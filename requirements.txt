# Core dependencies
ultralytics>=8.0.0
supervision>=0.12.0
opencv-python>=4.8.0
pillow>=9.0.0
numpy>=1.21.0

# GUI support
tkinter  # Usually comes with Python, but listed for reference

# Optional but recommended
torch>=2.0.0  # For GPU acceleration (install based on your system)
